---
const { title, useMonoFont = false } = Astro.props;
---

<details
  class="border border-gray-400 dark:border-gray-200 bg-gray-100 dark:bg-gray-800 px-5 py-3 rounded-md group"
>
  <summary
    class={`flex items-center justify-between gap-2 cursor-pointer select-none list-none py-1 ${useMonoFont ? 'font-mono' : ''}`}
  >
    <span>{title}</span>
    <svg
      class="transition-transform duration-300 shrink-0 group-open:rotate-90"
      xmlns="http://www.w3.org/2000/svg"
      width="16"
      height="16"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      stroke-width="2"
      stroke-linecap="round"
      stroke-linejoin="round"
    >
      <path d="M9 18l6-6-6-6" />
    </svg>
  </summary>

  <div class="pt-3 prose prose-sm max-w-none dark:prose-invert [&>ul]:my-2 [&>ul]:list-disc [&>ul]:pl-6">
    <slot />
  </div>
</details>
